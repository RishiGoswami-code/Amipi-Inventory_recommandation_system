# URL of the deployed FastAPI backend (Render, Railway, or any host).
# Local dev: leave unset — defaults to http://localhost:8000
# Production (Vercel): set this in Project Settings > Environment Variables
# to your backend's public URL, no trailing slash.
VITE_API_URL=https://your-backend.onrender.com
